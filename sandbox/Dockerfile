# SkyLog Sandbox — AI agent execution environment
# TODO: implement when agent runtime is designed

FROM node:22-alpine

WORKDIR /workspace

# Placeholder — actual image will be built in a later sprint
CMD ["sh", "-c", "echo 'SkyLog sandbox ready' && tail -f /dev/null"]